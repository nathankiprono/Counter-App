@model List<int>


    <div class="text-center">
@if (Model.Count == 0)
{
    <p>No results found.</p>
}
else
{
    <div>
        @foreach (var value in Model)
        {
            <div>Counter: @value</div>
        }
    </div>
}
    </div>

